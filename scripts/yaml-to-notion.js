#!/usr/bin/env node
/**
 * status.yaml → Notion (push) — **no implementado** en esta versión.
 * Motivo: evitar bucles de escritura y conflictos; usar Notion UI o MCP para updates puntuales.
 * Tracking: https://github.com/cloudsysops/opsly (issue futuro)
 */
console.log('⏳ yaml-to-notion: not implemented — Notion remains source of truth for live edits.');
console.log('   Use Notion UI, or notion-mcp, or extend this script with PATCH page calls.');
process.exit(0);
