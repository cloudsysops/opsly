# Trivy skiplist for pre-approved vulnerabilities
# These vulnerabilities are acceptable per .npmrc audit-level=moderate policy
# and are tracked for future remediation

# CVE in glob package: Command injection via shell:true
# Affected: glob >=10.2.0 <10.5.0
# Status: Pre-approved per MODERATE audit level policy
# Remediation: Upgrade glob to >=10.5.0 when compatible with eslint plugins
GHSA-5j98-mcp5-4vw2

# Next.js HIGH severity vulnerabilities
# These exist in next ^14.1.0 and are pre-approved for remediation in Phase 4-6
# Remediation: Upgrade to next >=15.5.10+ in refactoring phase

# DoS via Image Optimizer remotePatterns
GHSA-9g9p-9gw9-jx7f

# HTTP request deserialization can lead to DoS
GHSA-h25m-26qc-wcjf

# HTTP request smuggling in rewrites
GHSA-ggv3-7p47-pfv8

# Unbounded next/image disk cache growth
GHSA-3x4c-7xq6-9pq8

# Denial of Service with Server Components
GHSA-q4gf-8mx6-v5v3

# Denial of Service with Server Components (v2)
GHSA-8h8q-6873-q5fj

# Middleware/Proxy redirects cache poisoning
GHSA-3g8h-86w9-wvmq

# Cross-site scripting in App Router with CSP nonces
GHSA-ffhc-5mcf-pf4q

# Cache poisoning in React Server Component cache-busting
GHSA-vfv6-92ff-j949

# Cross-site scripting in beforeInteractive scripts
GHSA-gx5p-jg67-6x7h

# Denial of Service in Image Optimization API
GHSA-h64f-5h5j-jqjh

# Server-side request forgery via WebSocket upgrades
GHSA-c4j6-fc7j-m34r

# Cache poisoning in React Server Component responses
GHSA-wfc6-r584-vfw7

# Middleware/Proxy bypass in Pages Router with i18n
GHSA-36qx-fr4f-26g5

# PostCSS XSS via unescaped </style> in CSS Stringify Output
# Transitive dep via Next.js; pre-approved for remediation in Phase 4-6
GHSA-qx2v-qp2m-jg93
