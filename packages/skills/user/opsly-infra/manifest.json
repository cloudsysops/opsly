{
  "name": "opsly-infra",
  "version": "1.0.0",
  "description": "Docker, Docker Compose, VPS, deploy, Traefik, scripts bash, y operaciones de infraestructura. Fusión de bash+simplify. Usar cuando se mencione docker, compose, deploy, vps, ssh, script, bash, dockerfile, healthcheck, memory, infra.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "target": { "type": "string", "enum": ["script", "dockerfile", "compose", "vps", "deploy"] }
    },
    "additionalProperties": true
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "validated": { "type": "boolean" }
    }
  }
}
