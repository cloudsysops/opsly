{
  "name": "opsly-billing",
  "version": "1.0.0",
  "description": "Facturación Stripe en Opsly: suscripciones, invoices, metering y webhooks por tenant.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "scope": {
        "type": "string",
        "enum": ["subscriptions", "metering", "invoice", "webhook"]
      },
      "tenantSlug": {
        "type": "string"
      }
    },
    "additionalProperties": true
  }
}
