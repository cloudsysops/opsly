# Baseline de producción — inventario tenants (Opsly)

**Documento único (editar allí):** [`docs/TENANT-PRODUCTION-BASELINE.md`](../TENANT-PRODUCTION-BASELINE.md)

Esta ruta existe solo como ancla en la wiki de infraestructura; evita duplicar tablas que desincronizan con `opsly.config` y `system_state`.
