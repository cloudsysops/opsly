---
status: canon
owner: architecture
last_review: 2026-08-16
---

# Opsly Universe

Machine-readable narrative IP for Opsly. Implementation: `@intcloudsysops/universe` (`lib/universe`).

**Rule:** the Content Agent uses Universe. Universe does not depend on the Content Agent.

See `lib/universe/README.md` for API, CLI, and canon JSON under `config/universe/`.
