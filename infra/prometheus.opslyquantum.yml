# Prometheus mínimo para stack worker Linux opslyquantum (sin exporters extra).
# Ampliar con node_exporter en el host y targets reales cuando los añadas.

global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']
