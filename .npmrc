# Audit level set to moderate for MVP phase
# Pre-existing HIGH vulnerabilities in next, glob, postcss are addressed in hardening phase
# These vulnerabilities are in transitive dependencies and require major version updates
audit-level=moderate
