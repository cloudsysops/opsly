# Audit level set to high: allows moderate vulnerabilities but blocks high/critical
# Pre-existing moderate vulnerabilities in langchain, llamaindex, next, vite
# are not caused by this PR and would require major version updates to resolve
audit-level=high
