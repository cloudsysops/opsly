{
  "root": true,
  "extends": ["next/core-web-vitals"],
  "rules": {
    "prefer-const": "warn",
    "no-console": ["warn", { "allow": ["warn", "error"] }]
  },
  "overrides": [
    {
      "files": ["**/*.{ts,tsx}"],
      "rules": {
        "@next/next/no-img-element": "off"
      }
    }
  ]
}
