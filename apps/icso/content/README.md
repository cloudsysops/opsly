/** Sync: copy from config/commercial-catalog.json — keep identical. */
Source of truth for agents/CLI: `../../config/commercial-catalog.json`.
Site bundle copy: `commercial-catalog.json` (must stay identical — vitest enforces).
