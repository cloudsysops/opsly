// Short wiki URL — reuses the full knowledge base article page.
// The article page reads params.slug || params.id, so this works.
export { /* @next-codemod-error `default` export is re-exported. Check if this component uses `params` or `searchParams`*/
default } from "@/app/(prospect)/portal/learning/knowledge-base/[id]/page";
