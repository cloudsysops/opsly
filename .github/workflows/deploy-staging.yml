# Obsoleto como pipeline principal: el deploy staging automático vive en `deploy.yml`
# (push a la rama `staging` → job `deploy-staging`).
#
# Este workflow queda solo para recordatorio / ejecución manual opcional.
name: Deploy staging (info)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Documentación
        run: |
          echo "Deploy staging automático: push a la rama 'staging' (workflow Deploy)."
          echo "Ver docs/CICD-VPS.md"
