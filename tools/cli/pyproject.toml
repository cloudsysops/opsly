[project]
name = "opsly-cli"
version = "1.0.0"
description = "Opsly CLI Agent with multi-provider support"
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.0.0",
  "anthropic>=0.18.0",
  "openai>=1.0.0",
  "rich>=13.0.0",
  "typer>=0.9.0",
]

[project.scripts]
opsly = "tools.cli.main:app"
