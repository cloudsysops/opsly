---
agent_role: executor
max_steps: 5
goal: Create a simple test function
---

Create a TypeScript file src/test-function.ts that exports:
- A function `add(a: number, b: number): number` that returns a + b
- Proper types on all parameters
- Export as default

Keep it minimal and focused.
