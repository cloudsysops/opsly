# Agent Services Configuration
# Defines endpoints and capabilities for local agent services (Cursor, Claude, Copilot, etc)
# Workers use this config to locate and call agent services via HTTP

services:
  # Cursor IDE Agent Service
  # Runs on MacBook, listens on port 5001
  # Receives: prompt file path + content
  # Returns: execution result from Cursor IDE
  cursor:
    enabled: true
    url: "http://localhost:5001"
    env_var: "OPSLY_CURSOR_AGENT_URL"
    type: "http"
    agent_role: "executor"
    capabilities:
      - code-generation
      - typescript-execution
      - file-creation
    timeout_ms: 60000
    retry_attempts: 3
    retry_backoff_ms: 1000
    health_check_interval_ms: 30000
    description: "Cursor IDE local execution service"

  # Claude API Agent Service
  # Direct calls to Claude API via HTTP
  # Receives: prompt content
  # Returns: Claude completion
  claude:
    enabled: true
    url: "http://localhost:5002"
    env_var: "OPSLY_CLAUDE_AGENT_URL"
    type: "http_api"
    agent_role: "architect"
    model: "claude-opus-4"
    capabilities:
      - architecture-design
      - code-review
      - planning
    timeout_ms: 30000
    retry_attempts: 2
    retry_backoff_ms: 2000
    health_check_interval_ms: 60000
    description: "Claude API bridge service"

  # GitHub Copilot Chat Service
  # Optional: Copilot Chat API if available locally
  copilot:
    enabled: true
    url: "http://localhost:5003"
    env_var: "OPSLY_COPILOT_AGENT_URL"
    type: "http"
    agent_role: "executor"
    capabilities:
      - code-generation
      - code-completion
    timeout_ms: 45000
    retry_attempts: 2
    retry_backoff_ms: 1500
    health_check_interval_ms: 45000
    description: "GitHub Copilot local CLI bridge service"

  # Vercel OpenCode API Service
  # Optional: OpenCode endpoint for advanced code generation
  opencode:
    enabled: true
    url: "http://localhost:5004"
    env_var: "OPSLY_OPENCODE_AGENT_URL"
    type: "http"
    agent_role: "executor"
    capabilities:
      - advanced-code-generation
      - multi-file-generation
    timeout_ms: 120000
    retry_attempts: 1
    retry_backoff_ms: 3000
    health_check_interval_ms: 60000
    description: "OpenCode local CLI bridge service"

  # Codex/OpenAI-compatible local service
  # Disabled until a concrete local bridge is running.
  codex:
    enabled: true
    url: "http://localhost:5005"
    env_var: "OPSLY_CODEX_AGENT_URL"
    type: "http"
    agent_role: "executor"
    capabilities:
      - code-generation
      - repository-editing
      - terminal-assisted-development
    timeout_ms: 120000
    retry_attempts: 1
    retry_backoff_ms: 3000
    health_check_interval_ms: 60000
    description: "Codex local CLI bridge service"

  # OpenAI IDE fallback bridge (alias kept separate for operator routing)
  openai:
    enabled: true
    url: "http://localhost:5006"
    env_var: "OPSLY_OPENAI_AGENT_URL"
    type: "http"
    agent_role: "executor"
    capabilities:
      - code-generation
      - reasoning
    timeout_ms: 120000
    retry_attempts: 1
    retry_backoff_ms: 3000
    health_check_interval_ms: 60000
    description: "OpenAI/Codex local CLI bridge service"

  # Hermes routing bridge for Opsly LLM control-plane tasks
  hermes:
    enabled: true
    url: "http://localhost:5007"
    env_var: "OPSLY_HERMES_AGENT_URL"
    type: "http"
    agent_role: "planner"
    capabilities:
      - routing
      - planning
      - cost-aware-llm
    timeout_ms: 120000
    retry_attempts: 1
    retry_backoff_ms: 3000
    health_check_interval_ms: 60000
    description: "Hermes local CLI bridge service"

  # Decepticon red-team/validation bridge
  decepticon:
    enabled: true
    url: "http://localhost:5008"
    env_var: "OPSLY_DECEPTICON_AGENT_URL"
    type: "http"
    agent_role: "validator"
    capabilities:
      - adversarial-review
      - validation
      - security-audit
    timeout_ms: 120000
    retry_attempts: 1
    retry_backoff_ms: 3000
    health_check_interval_ms: 60000
    description: "Decepticon local CLI review bridge service"

  # Aider git-aware coding bridge
  aider:
    enabled: true
    url: "http://localhost:5009"
    env_var: "OPSLY_AIDER_AGENT_URL"
    type: "http"
    agent_role: "executor"
    capabilities:
      - git-aware-editing
      - code-generation
      - refactoring
    timeout_ms: 300000
    retry_attempts: 1
    retry_backoff_ms: 3000
    health_check_interval_ms: 60000
    description: "Aider local CLI bridge service"

  # Goose general-purpose automation bridge
  goose:
    enabled: true
    url: "http://localhost:5010"
    env_var: "OPSLY_GOOSE_AGENT_URL"
    type: "http"
    agent_role: "tool"
    capabilities:
      - multi-step-automation
      - repository-inspection
      - tool-use
    timeout_ms: 300000
    retry_attempts: 1
    retry_backoff_ms: 3000
    health_check_interval_ms: 60000
    description: "Goose local CLI bridge service"

  # Playwright frontend QA bridge
  playwright:
    enabled: true
    url: "http://localhost:5011"
    env_var: "OPSLY_PLAYWRIGHT_AGENT_URL"
    type: "http"
    agent_role: "validator"
    capabilities:
      - browser-e2e
      - frontend-regression
      - screenshot-validation
    timeout_ms: 300000
    retry_attempts: 1
    retry_backoff_ms: 3000
    health_check_interval_ms: 60000
    description: "Playwright local E2E and browser QA runner"

# Default service selection rules
defaults:
  # If no agent_role specified in prompt, use default
  default_agent: "cursor"

  # Fallback chain: if cursor unavailable, try claude
  fallback_chain:
    - "cursor"
    - "claude"
    - "copilot"
    - "opencode"
    - "codex"
    - "openai"
    - "hermes"
    - "decepticon"
    - "aider"
    - "goose"
    - "playwright"

# Scaling configuration (for future remote/Docker deployments)
# Example: Move Cursor to different machine by changing URL
environments:
  # Mock services for local testing (no real agents needed)
  mock:
    cursor: "http://localhost:5001"
    claude: "http://localhost:5002"
    copilot: "http://localhost:5003"
    opencode: "http://localhost:5004"
    codex: "http://localhost:5005"
    openai: "http://localhost:5006"
    hermes: "http://localhost:5007"
    decepticon: "http://localhost:5008"
    aider: "http://localhost:5009"
    goose: "http://localhost:5010"
    playwright: "http://localhost:5011"

  local:
    cursor: "http://localhost:5001"
    claude: "http://localhost:5002"
    copilot: "http://localhost:5003"
    opencode: "http://localhost:5004"
    codex: "http://localhost:5005"
    openai: "http://localhost:5006"
    hermes: "http://localhost:5007"
    decepticon: "http://localhost:5008"
    aider: "http://localhost:5009"
    goose: "http://localhost:5010"
    playwright: "http://localhost:5011"

  tailscale:
    cursor: "http://opsly-mac2011:5001"     # MacBook via Tailscale
    cursor_backup: "http://opsly-admin:5001"  # Admin machine backup
    claude: "http://opsly-admin:5002"       # Claude on admin machine
    codex: "http://opsly-admin:5005"
    hermes: "http://opsly-admin:5007"
    decepticon: "http://opsly-admin:5008"
    aider: "http://opsly-admin:5009"
    goose: "http://opsly-admin:5010"
    playwright: "http://opsly-admin:5011"
    # Orchestrator stays on VPS via normal config

  remote:
    cursor: "http://192.168.1.100:5001"  # Different machine
    claude: "http://192.168.1.100:5002"
    codex: "http://192.168.1.100:5005"
    hermes: "http://192.168.1.100:5007"
    decepticon: "http://192.168.1.100:5008"
    aider: "http://192.168.1.100:5009"
    goose: "http://192.168.1.100:5010"
    playwright: "http://192.168.1.100:5011"

  docker:
    cursor: "http://cursor-container:5001"
    claude: "http://claude-container:5002"
    codex: "http://codex-container:5005"
    hermes: "http://hermes-container:5007"
    decepticon: "http://decepticon-container:5008"
    aider: "http://aider-container:5009"
    goose: "http://goose-container:5010"
    playwright: "http://playwright-container:5011"
